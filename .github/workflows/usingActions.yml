name: using-actions

on:
    push

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
            - run: ls
            - uses: actions/setup-node@v4
              with:
                node-version: 22
            - run: node --version
